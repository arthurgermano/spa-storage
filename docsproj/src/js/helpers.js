
// ------------------------------------------------------------------------------------------------

export const assign = (target, source) => {
  return Object.assign(structuredClone(target), structuredClone(source));
};

// ------------------------------------------------------------------------------------------------
